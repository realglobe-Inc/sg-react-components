/**
 * Test case for sgThemeStyle.
 * Runs with mocha.
 */
'use strict'

const sgThemeStyle = require('../lib/sg_theme_style.js')
const assert = require('assert')
const co = require('co')

describe('sg-theme-style', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg theme style', () => co(function * () {

  }))
})

/* global describe, before, after, it */
