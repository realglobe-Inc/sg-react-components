/**
 * Test case for sgHeader.
 * Runs with mocha.
 */
'use strict'

const sgHeader = require('../lib/sg_header.js')
const assert = require('assert')
const co = require('co')

describe('sg-header', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg header', () => co(function * () {

  }))
})

/* global describe, before, after, it */
