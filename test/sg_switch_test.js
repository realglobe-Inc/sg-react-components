/**
 * Test case for sgSwitch.
 * Runs with mocha.
 */
'use strict'

const sgSwitch = require('../lib/sg_switch.js')
const assert = require('assert')
const co = require('co')

describe('sg-switch', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg switch', () => co(function * () {

  }))
})

/* global describe, before, after, it */
