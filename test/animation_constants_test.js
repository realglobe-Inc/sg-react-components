/**
 * Test case for animationConstants.
 * Runs with mocha.
 */
'use strict'

const animationConstants = require('../lib/constants/animation_constants.js')
const assert = require('assert')
const co = require('co')

describe('animation-constants', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Animation constants', () => co(function * () {

  }))
})

/* global describe, before, after, it */
