/**
 * Test case for sgPage.
 * Runs with mocha.
 */
'use strict'

const sgPage = require('../lib/sg_page.js')
const assert = require('assert')
const co = require('co')

describe('sg-page', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg page', () => co(function * () {

  }))
})

/* global describe, before, after, it */
