/**
 * Test case for sgBody.
 * Runs with mocha.
 */
'use strict'

const sgBody = require('../lib/sg_body.js')
const assert = require('assert')
const co = require('co')

describe('sg-body', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg body', () => co(function * () {

  }))
})

/* global describe, before, after, it */
