/**
 * Test case for sgVideo.
 * Runs with mocha.
 */
'use strict'

const sgVideo = require('../lib/sg_video.js')
const assert = require('assert')
const co = require('co')

describe('sg-video', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg video', () => co(function * () {

  }))
})

/* global describe, before, after, it */
