/**
 * Test case for sgButton.
 * Runs with mocha.
 */
'use strict'

const sgButton = require('../lib/sg_button.js')
const assert = require('assert')
const co = require('co')

describe('sg-button', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg button', () => co(function * () {

  }))
})

/* global describe, before, after, it */
