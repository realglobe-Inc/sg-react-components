/**
 * Test case for sgHead.
 * Runs with mocha.
 */
'use strict'

const sgHead = require('../lib/sg_head.js')
const assert = require('assert')
const co = require('co')

describe('sg-head', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg head', () => co(function * () {

  }))
})

/* global describe, before, after, it */
