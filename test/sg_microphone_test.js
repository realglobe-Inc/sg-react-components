/**
 * Test case for sgMicrophone.
 * Runs with mocha.
 */
'use strict'

const sgMicrophone = require('../lib/sg_microphone.js')
const assert = require('assert')
const co = require('co')

describe('sg-microphone', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg microphone', () => co(function * () {

  }))
})

/* global describe, before, after, it */
