/**
 * Test case for drawHelper.
 * Runs with mocha.
 */
'use strict'

const drawHelper = require('../lib/helpers/draw_helper.js')
const assert = require('assert')
const co = require('co')

describe('draw-helper', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Draw helper', () => co(function * () {

  }))
})

/* global describe, before, after, it */
