/**
 * Test case for sgMain.
 * Runs with mocha.
 */
'use strict'

const sgMain = require('../lib/sg_main.js')
const assert = require('assert')
const co = require('co')

describe('sg-main', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg main', () => co(function * () {

  }))
})

/* global describe, before, after, it */
