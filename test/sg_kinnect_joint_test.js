/**
 * Test case for sgKinnectJoint.
 * Runs with mocha.
 */
'use strict'

const sgKinnectJoint = require('../lib/sg_kinnect_joint.js')
const assert = require('assert')
const co = require('co')

describe('sg-kinnect-joint', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg kinnect joint', () => co(function * () {

  }))
})

/* global describe, before, after, it */
