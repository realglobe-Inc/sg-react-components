/**
 * Test case for sgHeart.
 * Runs with mocha.
 */
'use strict'

const sgHeart = require('../lib/sg_heart.js')
const assert = require('assert')
const co = require('co')

describe('sg-heart', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Sg heart', () => co(function * () {

  }))
})

/* global describe, before, after, it */
